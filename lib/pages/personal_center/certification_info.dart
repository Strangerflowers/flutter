import 'package:flutter/material.dart';

class CertificationInfo extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Scaffold(
      appBar: AppBar(
        title: const Text('认证资料'),
      ),
     body: Text('认证资料')
    );
  }
}
